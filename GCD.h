#ifndef GCD_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define GCD_H
#include <vector>

using namespace std;

int gcd(int a, int b);
int findGCD(vector<int> arr, int n);

#endif

